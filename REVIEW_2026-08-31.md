# balun 설계 심층 검토 — 2026-08-31

## 결론

- `balun_eth_rj45` Rev B의 현재 회로 연결, 배선, 규칙과 inner GND plane은 의도한 상대 비교용 2-port VNA fixture 구조와 일치한다.
- `balun_slipring/molex_end`의 논리 핀맵과 RF core는 타당하지만 실제 REV-504 connector identity와 mating은 미확인이다.
- `balun_slipring/m12_end`에는 제조사 female 8P 배열과 다른 치명적인 pad-number topology가 있었고 수정했다. 수정된 보드는 재배선·재생성 후 CAD 검사를 통과했다.
- 세 설계 모두 현재 CAD 기준 ERC 0, DRC 0, unconnected 0, schematic–PCB parity 0이다.
- JLCPCB 2026 공식 solver로 main 50 Ω geometry는 `49.9328–49.9955 Ω`, 100 Ω differential geometry는 `100.1103–100.2795 Ω`로 재검증됐다. 생산 보증은 nominal 계산 오차가 아니라 JLC 표준 `±10%`다.
- 2-port VNA로 S11/S21/S12/S22, NEXT/FEXT 전체 비교 측정이 가능하다. SMA 쪽 50 Ω load가 balun 뒤에서 nominal 100 Ω differential load로 보이므로 100 Ω 종단기를 자작할 필요는 없다.
- 이 결과는 전기적 CAD 정합성을 뜻할 뿐이다. slip-ring 두 endpoint는 실물 connector/continuity/mechanics와 측정 limit가 미확정이므로 계속 **DO NOT FABRICATE**다.

## 확정 수정 사항

1. Finecables M12 female 8P footprint를 제조사 권장 배열로 수정했다.
   - signal ring diameter: 5.5 mm
   - pin 8: center
   - key축 기준 pin 1/2: ±33°
   - pin 1–2 gap: 66°, 나머지 gap: 49°
2. 수정된 M12 pad 위치에 맞춰 TX/RX fan-in과 coupled trunk를 다시 배선했다.
   - TX: 28.060990/28.060991 mm, coupled 18.759529 mm, F.Cu, signal via 0/0
   - RX: 27.579764/27.579765 mm, coupled 18.110302 mm, F.Cu, signal via 0/0
3. slip-ring generator를 variant별 staging/rollback 구조로 변경했다.
   - live 회로도/PCB를 쓰기 전에 전체 산출물을 임시 sibling directory에 생성
   - native KiCad DRC, parity, zone refill+save 통과 후에만 live 파일 교체
   - 실패 주입 시험에서 live 파일 hash 변화 0 확인
4. RJ45 updater의 zone-fill 손실 문제를 수정했다.
   - 기존 `--routing-only`는 fill cache를 지운 뒤 성공해 plane 없는 Gerber를 만들 수 있었음
   - 현재는 변경 후 native zone refill+save, DRC/parity와 저장된 In1/In2 fill 존재를 강제
   - backup 범위에 SCH/PCB/PRO/DRU와 전체 custom footprint library 포함
5. SMA updater를 SCH/PCB staging+rollback 방식으로 변경했다.
6. CAD BOM field를 조달 manifest와 동기화했다.
   - RCT/RSH: UNI-ROYAL `0805W8F0000T5E`, `C17477`
   - TP1/TP2: Keystone `5001`, `C238122`
7. 문서의 근거 강도를 바로잡았다.
   - slide 14는 2세대 핀/선색 매핑 근거
   - `5055650501` exact MPN은 slide 15의 별도 4세대 표에서 가져온 교차 슬라이드 추론
   - `MB12MBAFF08ST-0`도 실제 DUT MPN이 아니라 catalog에 존재하는 후보
8. `PCB B면 배치`와 제조사 용어 `front-fastened panel`을 구분하고, sample 구매 순서·M3 CAD reference·crimp validation 수량을 정리했다.

## 회귀 검증 결과

| 프로젝트 | ERC | DRC | Unconnected | Schematic parity | 저장된 inner fill |
| --- | ---: | ---: | ---: | ---: | --- |
| `balun_eth_rj45` | 0 | 0 | 0 | 0 | In1/In2 각 1 |
| `balun_slipring_molex` | 0 | 0 | 0 | 0 | In1/In2 각 1 |
| `balun_slipring_m12` | 0 | 0 | 0 | 0 | In1/In2 각 1 |

KiCad 10 native dry-run으로 세 프로젝트의 Gerber, 분리 PTH/NPTH drill과 `--exclude-dnp` position CSV를 생성했다. 모든 명령은 exit 0이었다. `--check-zones` 출력의 In1/In2 Gerber는 각 파일에 region polygon을 포함했고, 저장된 fill만 사용하는 별도 no-check 출력에서도 동일하게 plane이 존재했다.

추가한 [`export_jlc_release.ps1`](export_jlc_release.ps1)은 live CAD를 해시로 보호한 staging copy에서 zone refill, ERC/DRC/parity, Gerber, PTH/NPTH drill/map, IPC-D-356, DNP-excluded JLC BOM/CPL과 SHA256 manifest를 재생성한다. 기본 모드는 RJ45 ZIP만 내고 slip-ring은 HOLD notice만 만든다.

Position CSV의 FIT reference는 다음과 같았다.

- RJ45: `J1,J2,J3,J4,J5,T1,T2,T3,T4,TP1,TP2`
- Molex endpoint: `J2,J3,T1,T2`
- M12 endpoint: `J2,J3,T1,T2`

따라서 slip-ring J1과 RCT1/RCT2는 현재 DNP/position-excluded 상태로 유지된다.

## 현재 의도와의 적합성

적합한 용도:

- 동일 fixture/설정에서 REF bypass와 slip ring의 상대 IL/RL/phase/group-delay 비교
- 정지 각도와 회전 중 변화 비교
- 상대 NEXT/FEXT 및 CT-FLOAT 대 CT-GND 민감도 확인

현재 구조만으로 주장할 수 없는 용도:

- 정식 100BASE-TX compliance 또는 합격 판정
- fixture가 제거된 절대 slip-ring S-parameter
- 순수 mixed-mode `Sdd/Sdc/Scd/Scc` 또는 CMRR

## RF/VNA 측정 결론

- Open, Short, Load는 각각 한 개만 있어도 Port 1/2에 순차 재사용할 수 있다. 다만 두 SMA-male cable end를 잇는 female-female thru가 필요하고 LibreVNA에서 `SOLT_12`를 활성화해야 한다.
- RJ45 두 보드는 미사용 SMA가 6개이므로 동일한 SMA-male 50 Ω terminator 6개가 동시에 필요하다. Slip-ring 두 endpoint는 2개가 필요하며 같은 6개 세트를 공유한다.
- Slip-ring의 모든 logical port pair는 6개 cable configuration, RJ45의 모든 8-port pairwise proxy는 28개 configuration으로 덮인다. 각 파일에 S11/S21/S12/S22 네 complex trace를 모두 저장한다.
- NEXT/FEXT는 DUT coupling이 fixture+REF floor보다 최소 10 dB, 가능하면 20 dB 커야 정량값으로 신뢰한다. 근접하면 upper bound로 표시하고 REF dB를 단순 감산하지 않는다.
- ADT2-1T+ 두 개가 through 경로에 들어가므로 typical transformer loss만 약 `0.96 dB @100 MHz`, `1.38 dB @200 MHz` 누적될 수 있다. 1–200 MHz를 기본 characterization, 200–450 MHz를 exploratory로 분리한다.
- 2-port balun 지그로 mode conversion, TCL/LCL, common-mode S-parameter, 정식 cable certification과 짧은 회전 dropout을 직접 판정할 수 없다.

전체 연결표와 설정은 [`VNA_TEST_PLAN.md`](VNA_TEST_PLAN.md)를 따른다.

## 확인된 지그/제조 caveat

1. RJ45 J1 앞 0.15 mm neckdown은 local 141–144 Ω 구간이다. 3.04/5.07 mm로 짧아 1–200 MHz relative fixture를 폐기할 정도는 아니지만 pair 내 비대칭과 connector fan-out이 mode conversion/NEXT floor에 포함된다.
2. Edge-launch SMA pad는 routed edge에 의도적으로 닿는다. JLC Remark에 pull-back/trim 금지를 넣고 `Confirm Production File=Yes`로 CAM을 승인해야 한다.
3. SMA 권장 PCB 두께는 `1.6±0.05 mm`지만 JLC 일반 1.6 mm 공차는 `±10%`다. 첫 bare PCB와 loose SMA sample-fit 또는 tighter-thickness engineering 확인이 필요하다.
4. 현재 별도 true 2x-thru/isolation coupon이 없다. Golden REF 반복측정이 당장의 authority이며, 더 낮은 crosstalk floor가 필요하면 `fixture_qa` coupon을 별도 설계하는 것이 좋다.
5. 2026-09-01 후속 결정으로 slip-ring RF50 경로는 transformer 쪽 early-jog와 SMA 직선 진입을 A/B 및 두 endpoint에 동일 적용했다. 각 경로는 `26.762102 mm`, H3/H4 중심 이격은 모두 6.0 mm다. 일반적인 금속 M3 head/washer 여유는 크게 개선됐지만 실제 hardware 외경과 장착 면 확인은 여전히 필요하다.

ADT2-1T+는 secondary dot이 pin 6이고 현재 P/N은 pin 4/6이므로 fixture 한 장에서 극성이 한 번 반전된다. 동일 fixture 두 장의 back-to-back 전달에서는 두 번 반전되어 상쇄되지만, 단일 fixture model/de-embedding과 절대 위상 부호에는 이를 반영해야 한다.

## 제작 전 남은 차단 항목

1. 실제 REV-504에서 Molex 1–4 ↔ M12 4–1 continuity, inactive pins 5–8 isolation, key/pin-1과 실제 endpoint MPN을 기록한다.
2. `5055680571` loose sample과 실제 housing을 체결하고 1:1 footprint, key와 pin 1을 확인한다.
3. `MB12FBAFF08ST-3` sample/preorder로 corrected PCB pattern, mating face, PCB B면 배치, front-fastened panel/nut/wrench access와 cable direction을 확인한다.
4. 2026-08-31 solver 검증값은 저장했지만, 주문 시점 JLC `JLC04161H-7628` live calculator 결과와 stack revision을 다시 보관하고 자동 stack/width 변경을 승인하지 않는다.
5. Keystone `5001` 권장 hole 1.02 mm와 현재 drill 1.00 mm의 finished-hole tolerance/retention을 확인하거나 실제 sample을 삽입한다.
6. SMA/RJ45/M12/transformer의 body 3D 또는 1:1 print와 실부품으로 board edge, enclosure와 fixture interference를 확인한다.
7. `MEASUREMENT.md`의 sweep, points, IFBW, power, averaging, calibration kit와 IL/RL/NEXT/FEXT/rotation/repeatability 합격선을 확정한다.
8. 2026-08-13 재고·가격 값은 역사적 snapshot이므로 주문 직전 다시 조회한다.

## 사용자 결정이 필요한 항목

- 목표가 `상대 characterization`인지 `100BASE-TX qualification`인지
- 기본 평가 대역과 200 MHz 초과 구간을 공식 판정에 포함할지
- 별도 `fixture_qa` 2x-thru/isolation coupon을 추가 발주할지
- 실제 장비에서 PCB 어느 면이 외부를 향하고 M12 panel/nut에 접근 가능한지
- 정량 IL/RL/NEXT/FEXT, 각도 변화, dropout과 반복성 limit
- 예상 fixture 탈착 횟수와 connector를 소모품/replaceable interposer로 만들지
- 사용할 calibration kit, coax/terminator ID, torque 관리 방식

위 결정과 실물 검증이 끝나기 전에는 release Gerber/BOM/CPL을 저장소의 제작 산출물로 배포하지 않는다.
